import React from 'react';
import { View, Text } from 'react-native';

const Main = () => (
  <View style={{ flex: 1, backgroundColor: 'green', marginTop: 64, alignItems: 'center', justifyContent: 'center' }}>
    <Text style={{ color: 'white' }}>
      I am the main screen
    </Text>
  </View>
);

export default Main;
