/*
*
*	{{properCase name}} - built using screen generator
*
*/

import * as actions from '~/constants/{{camelCase name}}';

export function defaultAction() {
  return {
    type: actions.DEFAULT_ACTION,
  };
}
