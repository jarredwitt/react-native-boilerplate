import {
  {{camelCase name}},
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = {{camelCase name}}();

describe('select{{properCase name}}', () => {

});
